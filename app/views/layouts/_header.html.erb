
    <% if signed_in? %>
<table width=100%>
<tr>
<td>

<%=render 'layouts/menu'%>

</td>
<td align=right>


  Welcome, <%=current_user.name%> | <%= link_to "Sign out", signout_path, method: "delete" %>




</td>
</tr>
<table>

<%=render 'static_pages/search'%>    
  
    <% #else %>
  <%#= link_to "Sign in", signin_path %>
  <% end %>
  
  
<hr>
	<font color=green>
      <% flash.each do |key, value| %>
	      <% if key == :lastaddedparticipant #special %>
	      	<input type=hidden id=participantaddedpopupmessage value="<%=flash[:lastaddedparticipant]%>">
	      <% elsif !(key == :notice && request['action']=='show')  %>
    	    <div class="flash-<%= key %>"><%= value %></div>
	      <% end %>
      <% end %>
  </font>

